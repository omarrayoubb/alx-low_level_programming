#include "main.h"
#include <string.h>
/**
 * _strlen - string len
 * @s: char
 * Description: char and string length
 *
 * Return: n the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
